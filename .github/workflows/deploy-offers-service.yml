name: Deploy offers-service

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      service_name:
        required: true
        type: string
      image_tag_suffix:
        required: true
        type: string
      sha:
        required: true
        type: string
    secrets:
      GCP_SA_KEY:
        required: true
      DATABASE_URL:
        required: true
    outputs:
      offers_service_url:
        description: "Deployed offers-service Cloud Run URL"
        value: ${{ jobs.deploy.outputs.offers_service_url }}

env:
  PROJECT_ID: project-f5f4f6f0-acae-485b-a16
  REGION: europe-central2
  REGISTRY: europe-central2-docker.pkg.dev
  REPOSITORY: mini-allegro
  OFFERS_IMAGE: offers-service

jobs:
  build:
    name: Build & Push offers-service image
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.sha }}

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGISTRY }} --quiet

      - name: Generate image tag
        id: meta
        run: |
          IMAGE_TAG="${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.OFFERS_IMAGE }}:${{ inputs.sha }}-${{ inputs.image_tag_suffix }}"
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push offers-service image (with cache)
        uses: docker/build-push-action@v6
        with:
          context: services/offers-service
          file: services/offers-service/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.image_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy offers-service
    needs: build
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    outputs:
      offers_service_url: ${{ steps.url.outputs.offers_service_url }}

    steps:
      - name: Prepare sanitized DATABASE_URL
        id: dburl
        env:
          RAW_DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          DB_URL="$RAW_DATABASE_URL"
          DB_URL="$(printf '%s' "$DB_URL" | sed -E 's/([?&])serverVersion=[^&]*(&|$)/\1\2/g; s/\?&/\?/g; s/&&+/&/g; s/[?&]$//')"

          if [ -z "$DB_URL" ]; then
            echo "Sanitized DATABASE_URL is empty"
            exit 1
          fi

          echo "::add-mask::$DB_URL"
          echo "value=$DB_URL" >> "$GITHUB_OUTPUT"

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Deploy offers-service to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ inputs.service_name }}
          region: ${{ env.REGION }}
          image: ${{ needs.build.outputs.image_tag }}
          env_vars: |
            DATABASE_URL=${{ steps.dburl.outputs.value }}
            PORT=8082

      - name: Health-check (HTTP 200)
        run: |
          URL=$(gcloud run services describe ${{ inputs.service_name }} \
            --region=${{ env.REGION }} \
            --format='value(status.url)')
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$URL/health")
          echo "Offers service URL: $URL"
          echo "HTTP Status: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "Health check failed! Got HTTP $STATUS"
            exit 1
          fi

      - name: Output service URL
        id: url
        run: |
          URL=$(gcloud run services describe ${{ inputs.service_name }} \
            --region=${{ env.REGION }} \
            --format='value(status.url)')
          echo "offers_service_url=${URL}" >> "$GITHUB_OUTPUT"
